[project]
name = "hki-example-bedrock-claude"
version = "0.1.0"
description = "HKI + AWS Bedrock (Claude) domain-scoped chat example."
requires-python = ">=3.11"
dependencies = [
    "hki-runtime>=0.1.0",
    "boto3>=1.34",
]

[tool.uv.sources]
hki-runtime = { path = "../../packages/hki-runtime-py" }
