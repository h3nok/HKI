[project]
name = "hki-example-langgraph-agent"
version = "0.1.0"
description = "HKI + LangGraph domain-scoped agent example."
requires-python = ">=3.11"
dependencies = [
    "hki-runtime>=0.1.0",
    "langgraph>=0.2",
    "langchain-core>=0.3",
]

[tool.uv.sources]
hki-runtime = { path = "../../packages/hki-runtime-py" }
