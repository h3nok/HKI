[project]
name = "hki-example-fastapi-rag"
version = "0.1.0"
description = "HKI + FastAPI domain-scoped RAG example."
requires-python = ">=3.11"
dependencies = [
    "hki-runtime[fastapi]>=0.1.0",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
]

[tool.uv.sources]
hki-runtime = { path = "../../packages/hki-runtime-py" }
